<?php

/*
|--------------------------------------------------------------------------
| 验证open请求是否合法
|--------------------------------------------------------------------------
|
| 输入
| - $code
|
|
*/

  if (!in_array($code, $GLOBALS['open_code'])) {
      exit('open码错误');
  }
